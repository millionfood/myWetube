header.header 
    .header_container 
        .header_column
            a(href=routes.home) 
                i.fab.fa-youtube
        .header_column 
            form(action=routes.search) 
                input(type="text", name = "search",placeholder="검색")
                button  
                    i.fas.fa-search
        .header_column 
            ul
                if !user
                    li
                        a(href=routes.join) Join 
                    li
                        a(href=routes.login) Login 
                else
                    li
                        a(href=routes.upload) Upload
                    li
                        a(href=`${routes.user}${routes.userDetail(user.id)}`) Profile
                    li
                        a(href=routes.logout) Logout
                        
                
                